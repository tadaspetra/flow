node_modules
out
dist
coverage
dist-smoke
.env
.cursor/hooks/state/
src/renderer/styles/main.css

.claude/worktrees/*
# This repo is pnpm-only (AGENTS.md). Keep pnpm-lock.yaml tracked for
# reproducible CI installs; ignore the npm lockfile so it cannot be
# re-introduced accidentally.
package-lock.json
